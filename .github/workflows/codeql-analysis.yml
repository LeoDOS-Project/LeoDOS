name: "CodeQL Analysis: cFS-Bundle"

on:
  workflow_dispatch:

jobs:
  codeql:
    uses: nasa/cFS/.github/workflows/codeql-reusable.yml@main
    with:
      component-path: cFS
      make: make -j8
      test: true
