[package]
name = "leodos-analysis"
version = "0.1.0"
edition = "2021"
description = "On-board data analysis algorithms for LEO earth observation"

[dependencies]
libm = "0.2"
leodos-utils = { path = "../leodos-utils" }
zerocopy = { version = "0.8", features = ["derive"] }
