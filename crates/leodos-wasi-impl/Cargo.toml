[package]
name = "leodos-wasi-impl"
version = "0.1.0"
edition = "2021"

[dependencies]
leodos-libcfs = { path = "../leodos-libcfs" }
leodos-libwamr = { path = "../leodos-libwamr" }
heapless = "0.9.1"
libc = { version = "0.2", default-features = false }
