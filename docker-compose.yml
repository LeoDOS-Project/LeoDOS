version: '3.8'

services:
  cfs-build:
    build: .
    image: cfs-build:latest
    volumes:
      - .:/cFS
    working_dir: /cFS
    stdin_open: true
    tty: true
    environment:
      - CFE_DIR=/cFS/cfe
      - OSAL_DIR=/cFS/osal
      - PSP_DIR=/cFS/psp
      - BUILD_DIR=/cFS/build
